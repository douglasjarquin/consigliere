# Boss-private operational home — never tracked.
.env
data/
state/
config/
projects/
.no-mistakes/

# Executor worktrees (Agent isolation) — never tracked.
.claude/worktrees/

# Python bytecode caches (bin/cs-herdr-events.py) — build artifacts, never tracked.
__pycache__/
