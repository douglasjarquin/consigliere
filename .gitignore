# Boss-private operational home — never tracked.
data/
state/
config/
host/
projects/
.no-mistakes/
.made/evidence/

# Local review artifacts and macOS noise — never tracked.
.lavish/
.DS_Store

# Executor worktrees (Agent isolation) — never tracked.
.claude/worktrees/

# Python bytecode caches (bin/cs-detach.py) — build artifacts, never tracked.
__pycache__/

# codegraph's local index - a directory (or a symlink to a central per-user
# store) holding the graph database; codegraph's own .gitignore inside it
# only covers its own contents, never the project root.
.codegraph
