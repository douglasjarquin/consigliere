last-reviewed: 76355e2

## 2026-08-11 - reviewed 345de4e..76355e2 (30 commits)
Boss took every relevant commit as work now - eleven items across nine tasks, nothing deferred to the backlog; four commits were already handled here and ten skipped as triaged.
- 6c206ed (#1842) port-now: a keyed decision answered by the boss stayed permanently open in the OPEN DECISIONS fold whenever the answer kicked off work, because the worker's next event lands in a different key namespace and no `resolved [key=...]` for the decision key ever lands; `bin/cs-send.sh` has no close-at-answer path. Ported as `cs-send --resolve-key`: after a confirmed send, append the closing resolved line to this home's own status, refusing when the named key is not currently open, never closing on a failed or unconfirmed send, and exiting nonzero with the manual close command when the append fails after delivery. -> task cs-decision-close-at-answer
- d8bb074 (#1831) port-now, local half only: a capo request the capo already answered kept escalating forever, because `cs_pending_reply_maybe_escalate` appends a `blocked:` escalation line and nothing ever closes it when the record later resolves. Ported as a per-request escalation key plus a close-on-resolve appended only while that exact key is still open in the fold. The commit's other half (the procevent runner applying adapter results in code) does not bite consigliere: our runner keeps a non-terminal source armed with no handler re-arm dependency. -> task cs-decision-close-at-answer
- 4cdd0bd (#1847) port-now: durable parent-binding records parsed with bash `read` silently drop NUL bytes, and bash 3.2 truncates where 5.x splices, so a corrupt record could resolve to a home its bytes never name contiguously. Ported as a fail-closed NUL reject on pending-reply records before field parsing, in the same refusal bucket as other malformed records. -> task cs-decision-close-at-answer
- 8398d31 (#1853) port-now: a capo seeded for an existing or inherited domain assumed greenfield, because a handed-off backlog keeps only recent Done entries, so inherited queues over-represent plans and shipped work resurfaced as open. Ported as a record-intake step in `capo-provisioning`: classify greenfield vs inherited, reconcile inherited plans against origin/main plus the live deployment, and never carry a plan row for shipped work. -> task cs-capo-record-intake
- f9b9d43 (#2065) port-now, problem only: `bin/cs-wake-drain.sh` moves the queue aside before handling, so a turn that dies after the move silently loses those wakes. Upstream's fix is entangled with the arm/continuity layer consigliere dropped, so the port was a fresh design for the foreground-checkpoint model. Landed as the drain's own orphan-adoption replay, acknowledged by the drain's committed print rather than the post-handling acknowledgement this entry originally planned (`docs/supervision.md`, "Drain durability", owns the mechanism and that rationale). -> task cs-wake-drain-durability
- 7f05100 (#2102) + 85e750a (#2029) port-now, NBSP slice only: `_cs_composer_trim` uses `[[:space:]]`, which under `LC_ALL=C` never matches U+00A0, so a claude composer row padded with NBSP classifies pending forever and away-mode injection defers indefinitely - fails safe, but wedges. Ported as locale-safe Unicode-space normalization in the shared composer owner with byte-capture regressions under UTF-8 and `LC_ALL=C`. The rest of 7f05100 consolidates the multi-backend adapter matrix consigliere dropped (herdr is the only runtime here). -> task cs-composer-nbsp
- fc5f164 (#2116) port-now: `bin/cs-spawn.sh` bases the task branch on the clone's current HEAD, and the clone only refreshes after a merge wake this home saw, so a spawn can start on stale main. Ported as a bounded base-freshness check against origin before worktree creation, with `--base` still bypassing. -> task cs-spawn-base-freshness
- 2d2be63 (#1568) port-now: lifecycle commands ride the data plane - a routing-marked `/exit` through `cs-send` arrives as chat the agent reasons about instead of executing. Ported as a control plane for consigliere's actual surface (herdr, codex+claude): allowlisted interrupt/exit/relaunch verbs addressed to an exact task id with verified postconditions, and relaunch as a journaled transaction delegating the launch to `cs-spawn.sh --relaunch`, which adopts the recorded endpoint and worktree. -> task cs-agent-control-plane
- 5ade9ef (#1984) + 073cb30 (#1841) + 53ecbc7 (#2091) + 7f828ea (#2110) + 167ff42 (#1928) port-now as one group: upstream rebuilt its memory sweep as tiered decaying memory - pinned/aging/perishable entry markers, archive-not-delete into a never-injected archive, session-evidence-only reinforcement, read-before-write classification of every finding, a standing-decisions sweep, boss-gated over-budget offload, and a cascade sweeping every registered secondmate home against its own budget. Ported into `vault` with consigliere's standing no-skill-storage exclusion preserved (the archive is the only cold tier here; upstream's excluded-skill offload destination is not adopted). -> task cs-vault-tiered-memory
- 60eb534 (#1918) port-now: the deferred network stage published one aggregate elapsed pair, so a slow startup could not be attributed to a phase, host, or clone without a hand re-run. Ported as per-step timing records published beside the report, printed only by the on-demand report command, inert unless a run asks for them. -> task cs-startup-network-timings
- f74d9e4 (#2099) port-now: the session recap listed open decisions but did not walk the boss through them. Ported into `the-books` and `rundown`: after the recap, present open decisions one at a time, most impactful first, each with escalation-quality context, until none remain. -> task cs-books-guided-decisions
- 4b6b89d (#1860) skip, already handled: network checks off the session-start blocking path landed here as 63bfb70 (#48) with the same detached-worker shape.
- 833a9a2 (#1925) skip, already handled: changed-files-only local lint with full lint in CI landed here as 002ec70 (#49).
- 70aeba8 (#1865) skip, already handled: bootstrap's lavish-axi floor is already 0.1.46 (the 2026-08-11 session digest reports the installed 0.1.45 below it, proving the floor is live).
- 199cb79 (#1826) skip, already handled: the fleet-state-before-context digest ordering was ported with 345de4e in the previous batch and AGENTS.md section 3 already states it.
- fb368dc (#1836), 990753a (#1846), be32879 (#1927) skip: remote-secondmate-over-SSH subsystem; every capo here runs local on one Mac.
- 06b33aa (#1917) skip: grok hook-marker guards; grok is a dropped harness here (codex and claude only).
- fffe91e (#2005), e836a7f (#2117) skip: Pi TUI launch modes; Pi is a dropped harness.
- 9068958 (#2100) skip: Windows CI runners for Herdr automation; consigliere CI has no Windows lane and no need for one.
- 74230fc (#1997), 76355e2 (#2147) skip: upstream product-vision docs; consigliere is a personal rewrite with its own charter.
- 91fa548 (#1851) skip: repairs firstmate's old-vs-new binary parity shim, a test harness consigliere never ported.

## 2026-08-06 - reviewed 30b18b9..345de4e (11 commits)
Boss took all five relevant commits as work now, with nothing deferred to the backlog; the remaining six skipped as triaged.
GitHub was in an Actions outage during this batch (zero workflow runs created for this repo after 18:51Z), and the boss merged the previous PR without CI as an executive decision.
- 345de4e (#1798) port-now: the startup digest is delivered through a harness that truncates an oversized payload from the tail, and upstream really was truncated - a 70KB digest arrived as lines 1-435 of 578, cutting off before the live-task inventory, so that session took the helm without knowing what was live or where its endpoints were. Consigliere had the same shape: `bin/cs-session-start.sh` emitted CONTEXT before FLEET STATE, printed status tails with no per-line cap (an observed ~200-character `blocked:` line), and listed Done rows while bounding the whole backlog listing indiscriminately. Ported as FLEET STATE ahead of CONTEXT with the safety preamble pinned, the read-once contract promoted to its own section naming what voids it, a shared per-line cap owner used by both this digest and the wake digest's OPEN DECISIONS cut, and a backlog listing composed as a recovery input - no Done rows, every in-flight/held/blocked row in full, only the dispatchable-now listing bounded with an exact remainder count. -> task consigliere-startup-digest-truncation
- 2cf0283 (#1794) port-now: a harness that shows only a digest preview while persisting the full output leaves an agent acting on a partial startup read. Ported as the one added AGENTS.md section 3 line, folded into the same task as 345de4e since both edit the startup contract. -> task consigliere-startup-digest-truncation
- 3b6ee03 (#1781) port-now: session start relied on the agent choosing to run it, and upstream observed a session that followed the recap path and never took the helm until a later request forced it. Consigliere was weaker still - it had no session-open hook at all, only the AGENTS.md section 3 instruction, with `.claude/settings.json` and `.codex/hooks.json` wiring the turn-end guard alone. Ported as a session-open hook that RUNS the digest through a single owner for session-open source semantics, with the digest bounded as one child so a stalled probe emits a loud truncation banner instead of silence, and a re-emit path that still re-verifies lock ownership and still drains queued wakes. -> task consigliere-startup-hook-run
- ef2c3a2 (#1733) port-now: bootstrap checked that the axi-family tools were present but never that they were current, so an out-of-date build misbehaved silently instead of being reported as an upgrade request before dispatch. This commit also SUPERSEDES the rationale behind 4a9979a, which the 2026-08-05 batch backlogged: the floor is each tool's current latest published version, bumped deliberately, not the earliest release that satisfies some behavior. The existing backlog item was re-scoped to this policy and dispatched rather than filed again. -> task consigliere-axi-version-floors
- bf01a42 (#1737) port-now: the fleet-wide open-decision scan added in the 2026-08-05 batch re-reads and re-folds every task's entire lifetime status log on every wake drain, so its cost grows without bound with total log size - and the wake drain is the hot path at the start of every wake-handling turn. Ported as a per-status-file byte cursor plus persisted folded open set that folds only newly appended bytes, with the fold-line rule extracted so the full and incremental strategies cannot disagree, O(1) device+inode and size-shrink rotation detection, and checked reads that keep the already-trusted set on an I/O error. -> task consigliere-open-decision-cursor
- skip (6 commits): 6f0f87f (#1754) preempts remote reply long-polls that stalled session startup behind a serial job worker - part of the remote-secondmate-over-SSH subsystem this ledger skips as a group, since every capo here runs local on one Mac; 3a8d63e (#1778) and 5d77b48 (#1784) present and then rename the X/Discord public-mention surface to Relay in user-facing docs - X mode is a dropped subsystem; 930ca76 (#1786) adds the Muse Code crewmate adapter and 8ec3e94 (#1788) then trusts its settled session log as idle - consigliere's harness layer is codex and claude only; 8387039 (#1787) floors default-on herdr presentation projection at Herdr 0.8.0 because a plain focus-stealing close moves the boss's active workspace for ~140ms on earlier releases - projection and presentation paths are a dropped subsystem and consigliere has no projection code to gate.

## 2026-08-05 - reviewed 4ee4a0a..30b18b9 (19 commits)
Boss took both port-now recommendations (dispatched to soldiers on claude-opus-4-8) and the one backlog item; the rest skipped as triaged.
- bb352e7 (#1711) port-now: a `needs-decision`/`blocked` event buried under later unrelated status appends was only visible via the last-line wake annotation, so a still-open decision could be silently missed. Consigliere had the same gap: `status_open_decisions` (bin/cs-classify-lib.sh) existed and fed cs-fleet-view/cs-afk-return/cs-decision-hold, but bin/cs-wake-drain.sh never consulted it, so open decisions surfaced only at heartbeat. Ported as a fleet-wide scan printing a bounded OPEN DECISIONS section on every drain including the empty-queue fast path, plus the unreadable-file and symlink guards (cheap `[ -L ]`, no O_NOFOLLOW subprocess). -> task consigliere-wake-drain-open-decisions
- 7ef26c4 (#1710) port-now: teardown could remove a task while its no-mistakes run sat parked at a gate (upstream observed a 7h39m orphan holding a fleet slot) and could leave disowned processes with cwd under the worktree/tasktmp pinning CPU after removal. Consigliere's cs-teardown.sh had neither defense. Ported as two identity-scoped, idempotent pre-teardown steps - confirmed `no-mistakes axi abort` cd'd into the exact worktree, and an `lsof -a -d cwd` TERM/KILL sweep with pre-signal identity re-verification - with the branch+head run-attribution contract factored out of cs-crew-state.sh into a shared lib so both use one owner, and incomplete cleanup refusing fail-closed. -> task consigliere-teardown-abort-reap
- 4a9979a (#1701) backlog: bootstrap version floors for the axi CLIs (old gh-axi failed bare `--squash` merges quietly; tasks-axi 0.2.2 multi-id mv). Installed gh-axi 0.1.29 and tasks-axi 0.2.4 already clear the floors, so nothing bites today; future-proofing only. -> backlog consigliere-axi-version-floors
- 1cd97c0 (#1704) skip, already handled: test fixture tmproot helper self-deletes under command substitution (trap fires in the subshell) and the recreated root leaks. Verified consigliere's tests/lib.sh `cs_test_tmproot` has the identical bug on main - probe confirmed DELETED-after-subst - but open PR https://github.com/douglasjarquin/consigliere/pull/33 (checks green, awaiting boss merge) already fixes it the same way: $$-keyed registry surviving command substitution, cleanup armed at source time. UPDATE 2026-08-11: PR 33 closed unmerged; the fix landed independently as a0985e3, re-derived against current main rather than cherry-picked.
- 7809ab9 (#1576), 3d9d12d (#1609), 733a504 (#1623), e5e8a67 (#1639), c8edff3 (#1659), a83be60 (#1660), fc3684a (#1691), 1939785 (#1699), bea3d23 (#1709), 30b18b9 (#1727) skip as one group: the whole remote-secondmate-over-SSH subsystem (transport, provisioning, doctor, Herdr readiness gating, Aqua job worker, SSH dead-peer bounds, entrypoint symlink resolution, trace propagation, durable parent bindings). All capos here run local on one Mac. 30b18b9's local half (durable parent binding backing up a launch-time env var) is already handled differently: consigliere's capo pending-reply records are durable files carrying parent_home= (bin/cs-pending-reply-lib.sh), never env-resolved. Revisit the group only if the boss ever wants remote capos.
- 976d97f (#995) skip: default-off per-task W3C trace-context propagation. No collector or consumer anywhere in this fleet; personal tool. Revisit on demand.
- cf95112 (#1577) skip: tmux liveness misattributed a live Claude 2.1.220 pane because it read the rewritable process TITLE (`pane_current_command`). Consigliere reads herdr's native `agent get`/snapshot agent fields (bin/cs-herdr-lib.sh), which are structural and title-independent - already immune. The generalizable rule (harness-dependent checks need real-harness proof) already lives in consigliere's verified-facts discipline.
- d0461e4 (#1661) skip: false watcher-down banners under firstmate's Claude Stop-hook auto-arm model, where the watcher legitimately runs only between turns. Consigliere dropped the arm layer; its model is the foreground checkpoint plus a persistent monitor whose beat exists mid-turn, so the false-alarm shape is absent.
- 3089a57 (#1708) skip: herdr presentation spaces default-on. Projection/presentation paths are a dropped subsystem.
- 71f0b3f (#1724) skip: Pi Calm built-in tool-name collisions. Pi is a dropped subsystem.

## 2026-08-03 - reviewed 33a4287..4ee4a0a (1 commit)
Single-commit batch. Boss took the port and chose the strict form over the lighter one I recommended.
- 4ee4a0a (#1563) port-now: a ship task's delivery mode and yolo were derived silently from the `data/projects.md` registry twice, at two different moments - `bin/cs-brief.sh` read it to shape the brief's Setup, Rule 1, and Definition of done, and `bin/cs-spawn.sh` read it again to stamp `mode=`/`yolo=` into meta - with nothing cross-checking the two reads. A registry edit between scaffold and dispatch, or a hand-adjusted brief, silently diverges the worker's definition of done from the record consigliere supervises against; and posture was never a per-task decision, so shipping one task differently meant changing a project's standing posture for every future task. BOSS CHOSE STRICT: `--mode` required on ship scaffolds and `--mode` plus `--yolo` required on ship spawns and promotions, all closed-set validated, `--mode`/`--yolo` refused on scout and capo, `--yolo` refused outright in briefs (the worker never owns approval decisions), a machine-readable `Delivery contract: mode=<mode>` line written into the ship brief and cross-checked at spawn before the worktree exists, warn-and-launch for briefs predating that line, a scout recording no posture at all, and a stderr deviation notice when the explicit mode is less rigorous than the registry's standing posture so the registry stays advisory. I had recommended the lighter form (cross-check plus optional overrides, registry as default) on the grounds that both registered projects sit at no-mistakes +yolo; boss overruled that. NOT PORTED, out of scope by instruction: firstmate's conditional `no-mistakes-prod-only` registry policy and the `--raw` flag it needed - consigliere has no such policy. -> task consigliere-explicit-delivery-contract

## 2026-08-02 - reviewed cd73e75..33a4287 (3 commits)
Second batch of the day, triaged after PR #29 merged. Boss took the one port and both skips. Worked directly in an isolated worktree again: the main home's session lock was held by the boss's other (codex) consigliere session, verified live rather than stale, so this session stayed read-only for fleet state and only authored code.
- 88b2a94 (#1545) port-now: the session lock matched only the BASENAME of `ps -o comm=` when walking the ancestry for a harness, and Claude Code's native installer names the per-session executable by version, so `.../share/claude/versions/2.1.220` has a basename that identifies nothing. Two failures: a session cannot claim its home at all, and - the dangerous one - a LIVE session's lock reads as stale to a second session, which then takes it, putting two sessions on one fleet. Ported into bin/cs-lock.sh as whole-path-component matching over both the executable path and argv[0], with acquire and liveness sharing one predicate (they disagreed before, which is exactly how a live holder could be read as stale). Whole components only, so a `.claude/hooks` path and a `cs-claude-*.sh` script name do not false-positive. Verified it does NOT currently reproduce here (local claude reports comm=claude); the fix is for the version-named and Linux cases. PR https://github.com/douglasjarquin/consigliere/pull/30. Upstream's other half (an attached arm misreporting a delivered cycle as failed) is in the arm layer consigliere does not have.
- f5ab708 (#1544) skip: shards the portable serial lane across four CI runners because upstream's hit 19m04s against a 20-minute cap and was being cancelled. Consigliere's portable lane is 48 scripts and measured 4m45s on CI against the same cap, and bin/cs-test-run.sh deliberately owns "no shards, families, or backends". Its other half (replacing wall-clock sleeps in a fixture with a signal handshake after a loaded-machine race) is the same class of bug already fixed in tests/cs-procevent.test.sh this cycle.
- 33a4287 (#1495) skip: 177 added lines in the turn-end guard, essentially all Claude auto-arm epoch machinery (.claude-autoarm-epoch, CLAUDE_MODE, auto-arm owner locks). Consigliere has no auto-arm layer; the foreground checkpoint is its only wait shape. Same reason a117b41 was skipped on 2026-07-31.

## 2026-08-02 - reviewed 3772964..cd73e75 (8 commits)
Boss reviewed the triage batch and chose to port all six relevant commits, including the two I had recommended backlogging as one subsystem. Landed as one batch: PR https://github.com/douglasjarquin/consigliere/pull/29 (all six checks green, merged by the boss). Consigliere did the work directly in an isolated worktree rather than dispatching, because another live session held the fleet lock for this whole session and a lock-refused session must not spawn.
- 9fdef64 (#1407) port-now: section 7 let a boss instruction that completely invalidates work under validation keep the same task and soldier, while the adjacent rule flatly banned hand-editing, committing, aborting, or restarting during an active run, with no carve-out - so a soldier facing full invalidation had no sanctioned move. Ported into AGENTS.md as the supersession sequence (supported abort, confirm stopped via axi status, follow branch_sync.next_action for custody, rebuild from the correct pre-invalidation base so the obsolete run's pipeline-fix commits stay out of the deliverable, validate exactly once against the final head), with the existing ban now cross-referencing it instead of contradicting it.
- 68641a3 (#1447) port-now: nothing told a soldier what `--intent` had to carry into `no-mistakes axi run`, so it could pass a summary of its own diff and be reviewed against materially less than the contract it agreed to, silently dropping later clarifications, constraints, and exclusions. Ported into bin/cs-brief.sh's no-mistakes definition of done only; direct-PR and local-only never run the pipeline, and tests pin that separation.
- 66b0f77 (#1229) port-now, scoped to one of its four defects: teardown closed the recorded pane, discarded the result, and erased the task's status, turn-end, metadata, check sidecars, and watcher markers, with a second path (worktree directory already gone) that skipped straight to record removal with no pane reasoning at all. Ported as cs_herdr_pane_presence in bin/cs-herdr-lib.sh plus a confirmed-gone gate in bin/cs-teardown.sh before the first irreversible step. DIVERGENCE: --force still proceeds (refusing would deadlock cleanup exactly when herdr is unreachable) but now names the pane it could not prove gone. The focus-flash and projected-workspace halves do not apply (projection dropped); the SIGKILL-ownership and recursive-home-cleanup halves fix defects consigliere does not have - it has no pane-death SIGKILL escalation, its capo retirement refuses rather than recursing, and its one foreign-pid signal path already re-proves identity with a start-time and cmdline match, which is stronger than the upstream fix.
- 1e24757 (#1452) port-now: data/capos.md was parsed three different ways with no shared owner. Worse here than upstream reported - a registry with no trailing newline silently drops the LAST capo from every `while read` consumer, which defeated the duplicate-home refusal and let an id be rebound to a different home with no refusal at all; and a capo id may legally contain `.`, which was interpolated raw into `grep -E`, so retiring `a.b` also deleted a row for `axb`. Ported as bin/cs-capo-registry-lib.sh (one anchored parse, literal id matching, EOF-safe reads, duplicate-id refusal, symlink and unreadable refusals) with all ten parse sites converted. Read path keeps summary/projects/added optional deliberately, because the file is hand-editable and a stricter parser would make a hand edit unroutable - the same "a capo disappears" failure being fixed; malformed rows now surface visibly in the fleet review instead of vanishing.
- 8c21b10 (#1483) + cd73e75 (#1500) port-now as one subsystem: no way to wait on a blocking external process without holding a conversational turn, which made a Lavish review round-trip cost a whole turn in the foreground. Ported as bin/cs-procevent{,-lib,-lavish}.sh plus supervision, watcher, and capo-teardown integration - runner owns the blocking child as its own process-group leader, captures durably before publishing an identity-only `check:` wake, one machine-wide owner per realpath-keyed source, hardlink acknowledgement, adapter-owned terminal retirement. ~1900 lines against upstream's ~3450. DELIBERATE OMISSION: the entire cd73e75 watcher-surfacing fix, because consigliere's checkpoint polls the durable queue directly (bin/cs-watch-checkpoint.sh) and an existing test already proves a queued wake no watcher produced is delivered - porting it would add a second delivery path and a marker family teardown must track. Also omitted: transactional sweep-home/snapshot/restore/recursive preflight (capo homes are one level, never nested), the `list` subcommand, the bespoke Perl output filter, and legacy claim backfill.
- skip (2 commits): 000c1db (bind --backend to exact-task authority) - consigliere has no multi-backend layer or --backend flag, and the underlying lesson already lives in section 1's precedence rule; a805766 (quota-aware dispatch completion runway) - consigliere owns dispatch policy differently.

Process note: one test in the new process-event suite was flaky and was fixed rather than accommodated - it waited for the source registration to disappear and then asserted the claim was gone with no wait, but retirement drops those as two steps inside one lock hold, so it passed in isolation and failed under load. Separately, one full-suite failure early in the session never reproduced across eight later runs including two under deliberate CPU saturation, and was never identified; recorded here as unresolved rather than closed.

## 2026-07-31 - reviewed b29621b..3772964 (32 commits)
Boss reviewed the triage batch and chose to port all ten relevant commits, including the two I had recommended backlogging and the one I had flagged as their call. Landed as one batch: PR https://github.com/douglasjarquin/consigliere/pull/19 (all six checks green, awaiting boss merge). Consigliere did the work directly in an isolated worktree rather than dispatching, because codex was out of weekly usage and the fleet could not spawn.
- 56a7ac6 (#1286) port-now: a busy pane was unconditional, UNBOUNDED proof of liveness, and since a busy pane's hash keeps changing (ticking elapsed counter) the wedge timer was unreachable by construction, so a hung foreground tool call was invisible for as long as it hung (upstream saw 25h). Ported as CS_BUSY_TURN_MAX_SECS (default 3600) in bin/cs-watch.sh, bounding busy-with-no-completed-turn against state/<id>.turn-ended and routing past the bound into the existing wedge_timer_check. Inspection only; never interrupts or restarts. Capo panes exempt.
- 6ec5e08 (#1256) port-now: relative CS_HOME/CS_DATA_OVERRIDE/CS_STATE_OVERRIDE get baked into durable briefs and restated on launch lines that other processes run from a different cwd. Reproduced concretely: with CS_HOME=home the brief told the soldier to append status to `home/state/t1.status`, which the soldier would create inside its own worktree where consigliere never looks - every status event lost silently. Ported as cs_abs_path anchoring in bin/cs-root-lib.sh. Anchor-only by design: absolute paths pass through byte-identical (no cd, no symlink canonicalization, no existence requirement), and all 32 override combinations still match the inline reference.
- 41ffd45 (#1195) port-now: panes are created by the long-lived herdr daemon, which does not inherit consigliere's environment, so under a non-default CLAUDE_CONFIG_DIR a bare `claude` fell back to ~/.claude and launched unauthenticated. Ported as cs_harness_launch_env in bin/cs-harness-lib.sh, applied to all three claude roles. Resolves to the same store cs_harness_claude_json_path writes folder-trust into, so trust and credentials cannot diverge.
- f0d7cbe (#1328) port-now (scoped down, deliberate): herdr enforces no workspace-label uniqueness and cs_herdr_workspace_find took the first match. Consigliere's exposure is much narrower than upstream's - every ship/scout task gets a brand-new workspace from `worktree create`, so only the capo home lookup resolves by label. Ambiguity now refuses (rc=2, both candidate ids named) and never creates a third workspace. Upstream's launcher-identity resolver (HERDR_PANE_ID ancestry) was NOT ported: it solves a placement problem this architecture does not have.
- 96e027e (#1277) + daf6dce (#1281) port-now: new requirements arriving mid-validation were expanding the task under validation, while genuinely required downstream corrections were being misread as expansion. Ported as one paragraph in AGENTS.md section 7 Validate plus the ask-user-authority step-3 rewrite: judge fix scope by accepted product/engineering behavior rather than an anticipated file list, and treat stale final-diff/PR evidence correction as autonomous.
- 0bbb27b (#1275) port-now: new-project intake duplicated a domain an existing capo already owned, because absence from data/projects.md was read as evidence nobody owned it (a capo's clones live in its own home). Ported into the project-management skill, with cloning and registering added as named triggers in AGENTS.md sections 6 and 13.
- a24eac1 (#1272) port-now (boss's call, taken): hard rule 1 now permits one boss-approved concrete project operation, named in the moment by operation or by the files/directories it touches. Never inferred, never broadened, never standing, and it does not relax the force, stash, discard, unlanded-work, or merge boundaries. This also cleared a live contradiction: project-management's Remove section deferred to a guarded helper that was never built and to a prime-directive exception that did not exist.
- 79e62b8 (#1303) port-now (scoped far below upstream, deliberate): data/boss.md, boss-shared.md, and learnings.md are read in full at every session start of every home, and curated prose has no natural ceiling. Ported as CS_STARTUP_MEMORY_MAX_BYTES (default 8192) reported by bin/cs-session-start.sh plus the matching consolidation obligation in the vault skill. Reports, never truncates. Upstream's two-script budget subsystem plus verification doc was NOT ported: all three files are empty today, so AGENTS.md section 7 requires the direct path.
- 514f0ab (#1093) port-now (code only): cs-brief.sh built seven sections with `VAR=$(cat <<EOF ...)`, where the lexer tracks quote state through the heredoc body while scanning for the closing paren. Converted to `IFS= read -r -d ''`; generated briefs are byte-identical across all twelve mode/variant combinations. Boss ruled that consigliere does not target stock macOS Bash 3.2, so the upstream test guard and CI parse-scope changes were explicitly NOT ported and tests/cs-brief.test.sh was left untouched. Kept purely as a more resilient construct.
- skip (21 commits): 9ea1a1a, c0c0881, 3a112a1, 621299a (Pi/Calm - dropped); fbece9c (Grok Stop + tmux endpoint selectors), 5fca47f (x-mode) - dropped; 2459f77 (tmux teardown conformance) - dropped; fa0d85d, b6e351d, 99533c5, 28b02d2, f7d0d0a (quota-array dispatch and auth preflight) - consigliere owns dispatch policy differently; 7cbb3f6, a323c2b (subagent guard, MSYS watcher arm) - dropped arm layer, macOS-only; a117b41 (claude Stop auto-arm ancestry) - consigliere has no auto-arm and its lock is lsof-based, not ancestry-based; e5bd082 (backend config inheritance) - no backend abstraction here; c21bf54 (Bearings chat-only) - /the-books is already chat-only; e595611 + a53ffc1 (.gitignore config/ as a directory) - already correct here; a2d5f26 (behavioral test coverage sweep) - only two consigliere test files use source-text assertions; 96542a4 (semantic busy lifecycle state) - consigliere already reads herdr's native semantic busy state with a regex fallback, so upstream is catching up to us.

Difference worth keeping: upstream's new rule is "unknown is never idle". Consigliere treats a native-unknown pane with no busy-regex match as stale-eligible. That is safe here because stale means human inspection, not a kill - but it is a deliberate divergence, not an oversight.

- 3772964 (#1362) port-now, with a boss-directed carve-out: landed after the batch above was triaged, so it was held for an explicit decision rather than folded into the "do all" that predated it. Consigliere's standing rules are written by consigliere, so without a precedence rule one of them can outrank a clear current boss instruction and turn "do exactly this" into a refusal citing consigliere's own bookkeeping. Ported into AGENTS.md section 1: an explicit, concrete, current boss instruction outranks a conflicting self-written rule within that instruction's exact scope, with the guardrails carrying the weight (never inferred, broadened, applied by analogy, carried to another object, or converted into standing authority; ambiguity takes one clarification first; yolo is never a substitute; never above platform/system/developer instructions). DIVERGENCE FROM UPSTREAM, boss's call: section 7's red-PR ban is carved out entirely. Upstream first tried a narrow exception permitting a red merge when the failure was inherent to the delivery path, then replaced it with this general rule, which still leaves the ban reachable by explicit instruction. Here it is not - a failing check is evidence from outside consigliere rather than a rule consigliere wrote, so the precedence does not reach it. Section 7 repeats the exemption at the ban itself. Hard rule 1's project-operation exception now defers to this rule instead of restating its guardrails, so they have one owner and cannot drift.

## 2026-07-27 - reviewed 68ed7ed..b29621b (27 commits)
Boss reviewed the triage batch and chose to port all eight relevant commits, including three I had recommended backlogging or skipping. Landed as one batch: PR https://github.com/douglasjarquin/consigliere/pull/4 (merged).
- 792174d (#934) port-now: sessions dispatched speculative design scouts for questions existing evidence already answered, and auto-serialized any same-subsystem overlap. Ported into AGENTS.md section 7 intake: consult existing evidence first, never pair a presented solution with a parallel design exercise, and serialize only for a true semantic dependency.
- ec09871 (#945) port-now: reviewer labels such as security or required were being treated as authority to decide ask-user findings and to let fixes expand the accepted contract. Ported as the new agent-only `ask-user-authority` skill (single owner of the procedure) plus the AGENTS.md section 7 authority boundary, the section 1 cross-reference, and the section 13 trigger.
- 587f591 (#946) port-now: over-building wrappers and control planes for one-off operational work. Ported as two sentences in AGENTS.md section 7.
- 10ee779 (#969) port-now: routine no-action acknowledgements padded with commentary about unrelated session decisions. Ported into AGENTS.md section 9 as the canonical `Boss, taken care of.` reply.
- a7e01bc (#926) port-now: soldiers appended bare `working:` receipts to acknowledge a routed request. Ported into bin/cs-brief.sh: a marked request needs one correlated answer, and only a genuinely reported phase takes a stable key.
- cb3aa7b (#968) port-now: the session recap reported "nothing happened" while an older unanswered boss decision sat visible in history. Ported into the `rundown` skill, which now also sweeps the whole visible session for still-open decisions.
- b24e72c (#932) port-now: a merged PR's poll kept re-firing every watcher cycle until teardown, re-notifying the same merge. Ported as a deliberately minimal `cs_pr_poll_retire` in bin/cs-pr-lib.sh plus the cs-watch.sh call site: retirement runs only after the wake is durably queued, is identity-bound across the GitHub query, removes the runnable check first, and never touches task metadata. Upstream's 355-line crash-recovery receipt was NOT ported; a crash mid-retirement only reproduces today's duplicate-wake behavior plus orphan sidecars that teardown already removes.
- f017572 (#950) port-now (partial, deliberate): a kind=capo record with no recorded endpoint is now reported instead of skipped silently. Upstream's relaunch-missing-secondmate half does NOT port: a registered capo with no local record is the ordinary seeded-but-not-yet-launched state (an existing test requires sweep silence there), and AGENTS.md section 5 forbids sweeping herdr by name to find an endpoint, so a blind respawn could duplicate a live supervisor.
- skip (19 commits): ca4be1c, 65ad47b, 6b0d21d, 1c4d210 (Pi Calm mode - Pi dropped); aca3ad1, a5fe1bc (tmux composer/busy detection - tmux dropped); 3eca8ff, 2b7cd66 (herdr presentation projections - dropped); c64ad1c, 39b450b, 4d6992a (Kimi adapter), b29621b (pi-signed adapter) - consigliere's harness layer is codex/claude only; 418ab78, b05eb24 (Claude arm/continuity layer - consigliere's only wait shape is the foreground checkpoint); 3f71cdd, 5c89d36, 34213e6 (dispatch selector and quota-window rules - consigliere already owns dispatch policy differently); d89a1b6 (ShellCheck source-graph perf - our lint is a single simple script); 861b1f8 (docs audience split across dropped-subsystem docs).

Review process note: two defects in my own ports were caught by the test suite during development and fixed rather than accommodated - a capo sweep addition false-positived on the normal seeded state (removed entirely), and a pr_head assertion was wrong because cs-pr-check records pr_head only when gh supplies it (replaced with a byte-comparison proving retirement never modifies metadata). The pipeline review then found a narrow validate-to-unlink race in poll retirement; the boss accepted it as a documented limitation rather than adding a cross-script lock, on the evidence that the trigger needs a merged PR and a newly armed PR for the same task within microseconds, the worst case is one missed notification, and teardown independently verifies landing.

## 2026-07-23 - reviewed 5140413..68ed7ed (8 commits)
- 593e3a2 (#873) port-now: firstmate added a built-in ahoy session-recap skill; consigliere had only the heavy live-state brief. Ported as the new light `rundown` skill (session-only recap, falls back to the-books). -> task port-rundown-recap, PR #3
- 5549834 (#875) skip: preserves trustworthy data in partial Bearings snapshots. Consigliere has no snapshot/projection subsystem - cs-fleet-view is a live read, not a persisted partial-trust snapshot - so the bug does not exist here. Revisit if consigliere ever adds status-brief snapshots.
- 4497181 (#884) skip: Pi session-local calm mode. Pi is a dropped subsystem.
- 82a7943 (#885) skip: Pi redundant watcher re-arms. Pi plus the arm/continuity layer are dropped (consigliere's only wait shape is the foreground checkpoint).
- 6db3b09 (#895) skip: Pi Calm transcript rendering. Pi dropped.
- bd43c73 (#898) skip: GH Actions workflow fired only one PR-body compliance event. Consigliere has no .github/workflows; its gate is the no-mistakes pipeline plus boss merge.
- 6bcb381 (#899) skip: excludes operational injections from ahoy recap boundaries. No ahoy here; the generalizable residue (type operational injections, don't sniff prose) is subsumed by the #909 port.
- 68ed7ed (#909) port-now: canonical operational-input typing - a typed wire form for machine injections so classifiers identify them by structural type instead of prose. Consigliere shares the marker/send/classify/daemon subsystems and was still prose/label-based. Ported as bin/cs-operational-input.sh with the injectors refactored onto it. -> task port-operational-input, PR #2 (merged)

Note (not upstream-driven): alongside this batch the boss renamed the `bearings` status-brief skill to `the-books` (mafia vocabulary). -> task rename-bearings-thebooks, PR #1 (merged).
