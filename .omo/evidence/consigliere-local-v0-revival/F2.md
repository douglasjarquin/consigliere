# Final verification F2: code quality review

## Review target

The source review target is runtime commit `42933d103da9171c76b1564888e0d6557291fb5d` on `revival/v0-local-codex`, based on `24ffea8fa1f5bc983fb5965efab0a89b6116f05b`.

The later final-lane evidence commit changes documentation only and does not change packaged runtime inputs.

The review covers the full branch diff, all fifteen task commits, the follow-up hardening commits, the task evidence, and the packaged artifact produced from the target.

## Invariants checked

SQLite remains authoritative for durable state, while Git, filesystem, process, network, and Codex operations occur outside SQLite writer transactions through durable intent and reconciliation.

Project, Mission, Attempt, Workspace, lease, generation, fence, runner identity, invocation, and exact Git SHA identities are checked at each external boundary.

Home locking uses the kernel-managed lock before migrations, cleanup, listeners, schedulers, reconciliation, or runner startup.

Boss authority, Attempt capability authority, runner control authentication, model advisory authority, and delivery authority remain separate.

Dispatch has one recoverable Attempt slot, progression waits for verified runner death, and imported work is bound to one exact result ref.

Codex execution uses bounded fresh context and bounded usage records without native transcript resume or full transcript retention.

Verification commands are literal argv arrays with bounded count, timeout, environment, cwd, and output.

The new progression regression closes the previously observed gap where an ignored `AttemptResults.mark/3` error could leave an external result ref after a failed durable checkpoint.

The fixed code propagates terminal-sequence, death-verification, commit-verification, and imported-status persistence errors and stops before external import when the checkpoint cannot be recorded.

The verification child process now starts through `env -i` with only the explicit allowlisted environment, so an unlisted parent secret cannot cross into Project-configured commands.

Structured redaction now covers quoted token-like keys and `CS_CAPABILITY` without an unbounded key-matching expression, so oversized non-secret ContextPacks remain bounded.

Termination no longer trusts a persisted PGID by itself; it requires canonical inventory, matching Attempt identity and fence, verified runner liveness, and a matching persisted PGID before signaling.

Live inventory additionally requires the observed runner and harness PIDs to be members of the manifest's recorded process group, so a stale same-host process cannot make a live group appear verified.

Checkpoint import applies the complete trusted-workspace verifier before any external fetch, including exact HEAD, alternates, remotes, credential helpers, hooks, permissions, and shared-object checks.

The Attempt capability visible to the worker is the short-lived worker-scoped credential required by task #132, while the runner control-channel secret remains separate and private.

The harness environment now receives no daemon API socket or reusable Attempt capability; the trusted runner retains the reporting capability and forwards only bounded reports through the bridge.

Pause revokes live capabilities without rotating the persisted fence before the runner manifest is reconciled, so the live manifest remains bound to the Attempt during verified termination.

An explicit durable `pausing` blocker causes the MissionCoordinator to retry settlement on refresh, including after runner `DOWN` and daemon reconstruction, without pausing unrelated active Missions.

Redaction covers bounded compound secret assignments, private-key fields, and PEM blocks in addition to the existing token-shaped keys and sensitive structured values.

Provider-prefixed `OPENAI_API_KEY` and `ANTHROPIC_API_KEY` assignments are covered by the same fixed-family redaction path, and authenticated harness stderr is retained in the bounded Attempt log.

Runner stdout is assembled by complete newline-delimited records across transport chunks, with an explicit bounded discard path for oversized unterminated records.

Result idempotency compares the persisted `accepted_terminal_sequence`, so a changed terminal event cannot be accepted as a duplicate of an earlier report.

Unverified runner death holds the durable dispatch slot across scheduler restart until an independent death verification releases it.

Dispatch re-reads an Attempt after a stale spawn transition instead of crashing the MissionCoordinator or creating a second Attempt.

Protocol-failure termination is classified from both the persisted Attempt marker and the GenServer stop reason, preserving a semantic failure instead of downgrading it to `lost`.

The non-sandboxed SQLite test suite runs with one case at a time, and process-level report fixtures explicitly prepare their trusted workspace and trap the expected linked failure exit.

## Automated gates

The Linux CI-shaped command passed format, compile with warnings as errors, and the complete daemon suite with `482 passed (1 doctest, 481 tests)`.

The complete daemon suite passed three consecutive seed-0 runs after the final hardening.

The affected progression, exact-SHA, verification-environment, Attempt-result, reconciliation, fragmented-output, stream-sequence, pause, and redaction hardening slices passed within the same Linux path, and the complete suite covered the combined final head.

The CLI gate passed `gofmt`, `go vet`, ordinary tests, race and shuffle tests, and `go build ./cmd/cs ./cmd/csd`.

The runner gate passed `gofmt`, `go vet`, ordinary tests, race and shuffle tests, and `go build ./...`.

The final package gate produced native arm64 Mach-O `cs`, `csd`, `cs-runner`, and `cs-attempt` artifacts from runtime source head `42933d103da9171c76b1564888e0d6557291fb5d`, and `cs version --json` returned `{"cs":"0.1.0","protocol":1}`.

The package prefix was `.tmp/package-42933d1.mZakYT` with hashes `4b3891c4a27c3c21b10c8324627f2701288cdf83842f8a02619da61df2dd4a2c` for `cs`, `400d29f584f7ceacf225608cb28c25ddaf3c9846b19e8e8aa890496afa020b48` for `csd`, `fa583582034b6c3aa1c1831f387fe578519172990876c6cf4774c28b7b12a382` for `cs-runner`, and `9393d8fecbadaf680caec85f260443fed17c9a5bdaa76a3ad408d683d95b2acb` for `cs-attempt`.

The final installed lifecycle gate passed `csd migrate`, `csd start`, `cs ping`, `cs health`, `cs doctor`, `csd status`, `cs projects`, idempotent repeated stop, restart, ping, doctor, stop, and socket and PID absence assertions.

The exact commands, exit statuses, package hashes, native file types, and cleanup receipt are retained in `final-gate-receipt.md`.

The remote CI run for the final pushed head is recorded after the direct-PR push in the final evidence update.

## Known host-only observations

The full native macOS daemon suite ran 482 tests and reported 473 passing and nine environment-sensitive failures.

Two socket tests observed a stale probe socket during a timing-sensitive assertion.

Two runner environment tests exceeded macOS's short Unix-domain socket path limit under the deep temporary test path.

One chaos-security test observed the same stale probe-socket timing boundary.

Three reconciler process-group fixtures differed in macOS process inventory, cleanup timing, or shared test-runtime ordering.

Two Project tests compare `/var` with macOS's canonical `/private/var` spelling.

The exact native failure categories and bounded output summary are recorded in `macos-native-gate.md`; these host portability and fixture observations are not used as the release gate.

The Linux release gate is authoritative for the complete daemon suite, while the packaged macOS lifecycle and the selected canary passed on their respective real surfaces.

Verdict: PASS for the final source and package quality gate, with the named macOS full-suite portability observations retained for follow-up rather than hidden.

## Current exact-head refresh

This refresh supersedes the earlier runtime target and binds the code-quality verdict to full HEAD `8d839378a55e36222e13c19e84e1f91543fc92c4`.

The follow-up closes the three exact-head boundary findings: planned cancellation now releases the live scheduler cache only when no durable Attempt or unreleased dispatch remains, the model-advisory principal cannot invoke `attempt.logs`, and a failed durable `starting` to `running` transition tears down the authenticated runner before the Attempt is marked spawn-failed.

The focused RED/GREEN slice returned `10 passed`, and the complete Linux daemon suite returned `483 passed (1 doctest, 482 tests)` in three consecutive runs.

The exact current package hashes and installed lifecycle are recorded in `package-artifact-8d83937.log` and `installed-lifecycle-8d83937.log`.

The current native macOS characterization is `474/483` with nine named host-only failures, while the Linux container remains the authoritative release gate.

The current code-quality verdict is PASS for the exact local head, pending fresh remote CI evidence after push.

## Final exact-head refresh

This refresh supersedes the earlier runtime target and binds the code-quality verdict to full HEAD `bf22b5d4cae239a222a3065ca4b34b574dd676ad`.

The final Linux daemon gate returned `486 passed (1 doctest, 485 tests)` in three consecutive seed-0 runs after the runner-startup fixture alignment and fail-closed identity hardening.

The final CLI and runner Go gates passed formatting, vet, ordinary tests, race and shuffle tests, and builds.

The exact current package hashes and installed lifecycle are recorded in `package-artifact-bf22b5d.log` and `installed-lifecycle-bf22b5d.log`.

The native macOS full-suite characterization is recorded in `macos-native-gate-bf22b5d.log` with nine named host-only failures; the Linux container is the authoritative complete-suite release gate.

Final local verdict: PASS for source and package quality at the exact implementation head, pending fresh remote CI evidence after push.

## Current exact-source refresh

The reviewed runtime source is `71593738cf6aae723c9208743405fa12a9dc7a03` on `revival/v0-local-codex`, with historical base `24ffea8fa1f5bc983fb5965efab0a89b6116f05b`.

The fresh report `review-final-7159373-code.md` recommends approval and records no critical or high correctness, authority, persistence, identity, or lifecycle finding.

The Linux daemon gate passed `491 passed (1 doctest, 490 tests)` three times, the CLI and runner Go gates passed formatting, vet, ordinary tests, race and shuffle tests, and builds, and the package and installed lifecycle receipts passed.

The native macOS gate passed the complete daemon suite on this host.

The fresh review records oversized multi-responsibility modules and timer-sensitive lifecycle tests as medium or residual maintainability concerns, not unresolved correctness or security blockers.

The current local F2 verdict is APPROVE with those bounded residual risks explicitly retained.

## Current exact-source refresh

The runtime source head is `7c54c782552f3ee5a09ddee35735e90cba1b9339` on `revival/v0-local-codex`.

The authoritative Linux daemon gate and native macOS daemon gate each passed `496 passed (1 doctest, 495 tests)` at this source head.

The CLI and runner Go gates passed formatting, vet, ordinary tests, race and shuffle tests, and builds.

The package is native arm64 and the installed lifecycle passed identity-safe restart, repeated stop, and zero-residue cleanup.

The runtime audit follow-up records RED-to-GREEN closure for package binary compatibility, generation identity, terminal runner cleanup, bounded native observers, and start fingerprints.

The exact receipts are `runtime-audit-followup-7c54c78.md`, `package-artifact-7c54c78.md`, and `installed-lifecycle-7c54c78.md`.

A fresh final code review and runtime audit bind their verdicts to this full source SHA in separate reports.

## Closing exact-head delivery refresh

The fresh code review `review-final-7c54c78-code.md` is `WATCH` with recommendation `APPROVE` and no blockers.

The final runtime audit `runtime-audit-final-7c54c78.md` is PASS for the exact runtime source head.

The exact source gates remain green at `496 passed (1 doctest, 495 tests)` for both the authoritative Linux and native macOS daemon suites, with the CLI and runner Go gates also passing.

Remote CI run `33322422318` for PR head `85f02c5b739116d7de0d3f04a372f463bbb913e6` completed successfully with all five checks passing.

The only retained review risks are medium maintainability concerns for oversized modules and polling-heavy lifecycle tests, with no unresolved correctness, security-boundary, data-loss, concurrency, or lifecycle blocker.

The closing code-quality verdict is APPROVE with those bounded residual risks.

## Final pushed-head attestation

The final pushed head `c060b88035128bbdbf361f1bcab9a100521965e9` adds no runtime source changes after the reviewed source head `7c54c782552f3ee5a09ddee35735e90cba1b9339`.

Remote CI run `33322804969` passed all five checks for the final pushed head.

The final runtime audit, code review, and package QA reports remain bound to the exact runtime source, and the final gate review is PASS with no blockers.

The complete final delivery attestation is `final-delivery-attestation-c060b88.md`.

## Watcher follow-up exact-head code quality

The command verifier fix owns the bound at the receive boundary and only slices the bounded prefix in the overflow branch, avoiding unbounded materialization of the received chunk.

The ContextPack fix aligns the model instruction and completion metadata with the existing first-terminal result invariant.

The advisory fix uses a dedicated sanitizer for the newly authorized bounded read and leaves authority-bearing advisory operations denied.

Focused and full daemon, runner, CLI, package, and manual checks passed at runtime source `c727e94ae2bac1be0d3d33fc0005258e5fd850cd`.
