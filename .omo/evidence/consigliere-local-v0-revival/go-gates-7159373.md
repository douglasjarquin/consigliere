# Go gates at exact source head

Date: 2026-08-30.

Source: `71593738cf6aae723c9208743405fa12a9dc7a03` on `revival/v0-local-codex`.

The CLI command was:

```text
cd cli
test -z "$(gofmt -l .)"
go vet ./...
go test ./...
go test -race -shuffle=on -count=1 ./...
go build ./cmd/cs ./cmd/csd
```

The runner command was the equivalent gate in `runner/cs-runner`, ending with `go build ./...`.

Both commands exited `0`.

The gates covered formatting, vet, ordinary tests, race and shuffle tests, and native builds.
