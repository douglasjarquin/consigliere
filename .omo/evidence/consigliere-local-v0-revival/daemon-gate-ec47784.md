# Exact-head daemon gate receipt

Source head: `ec47784a801ee8168fae7b249bf3b8342951ae17`.

The daemon was formatted and compiled with warnings treated as errors before the complete test suite ran.

Result: `507 passed (1 doctest, 506 tests)`.

The focused Away cursor race and termination ordering regressions passed in five consecutive seed-0 runs.

No product process remained after the gate.
