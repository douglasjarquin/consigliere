# Final verification F4: scope fidelity

## Product exclusions

The final product does not add Capos, Secondmates, persistent per-repository managers, remote workers, multi-host coordination, another production harness, native Codex transcript resume, or a full-screen TUI.

It does not add Made-managed validation, AFK notifications, visible notification delivery, automatic GitHub delivery, product-created pull requests, pushes, merges, a telemetry service, a broad evidence warehouse, full transcript retention, or raw public canary data.

The packaged V0 path does not depend on the legacy Bash supervisor or interactive shell version management.

The current main-branch Bash fleet is not ported or broadened.

The product does not choose the canary Project, task sample, duration, Continue limit, fixed allocation, or comparison pairing.

The product has no automatic Promote path.

The exact-base release still compiles inherited modules for legacy Made, Fake harness, GitHub, delivery, Away, and notification paths because OTP bundles application modules.

This revival neither adds nor wires those inherited modules into the packaged V0 path, and deleting them would be an unrelated scope expansion.

The final hardening also keeps live process identity tied to the recorded process group and refuses checkpoint import from a workspace that fails the trusted Git verifier.

The final hardening keeps daemon transport and reusable Attempt capabilities out of the harness environment, keeps termination fences bound to live manifests, and redacts bounded compound secret and PEM forms.

## Canary ownership and evidence

The operator selected the noncritical dotfiles Project and naturally occurring removal task.

The optimized FirstMate configuration came from `/Users/douglasjarquin/github/douglasjarquin/dotfiles/files/firstmate/agent/config/crew-dispatch.json`, `/Users/douglasjarquin/github/douglasjarquin/dotfiles/files/firstmate/main/config/crew-dispatch.json`, and `/Users/douglasjarquin/github/douglasjarquin/dotfiles/files/firstmate/runner/config/crew-dispatch.json`, and resolved to Codex, `gpt-5.6-luna`, and `high` reasoning.

Consigliere recorded package, daemon, CLI, runner, Codex, sandbox, approval, context, and bounded token-counter identities in the private canary data and redacted report.

The canary ran one naturally selected Consigliere Mission with one explicit checkpoint continuation and zero FirstMate duplicate Missions.

The report records one human `Continue` decision, fewer than 20 naturally occurring comparable Missions, insufficient evidence for Promote, and no economic-superiority claim.

The operator retains the next Continue or Stop decision.

## Adversarial scope scan

Malformed inputs, prompt injection, stale identities, dirty or unsafe workspaces, hung commands, flaky timing, misleading output, cancellation boundaries, invalid results, duplicate dispatch, repeated interruption, and restart are exercised in the task records or explicitly recorded as not forced when doing so would create duplicate or non-natural work.

The advisory surface cannot authorize, pause, cancel, waive, deliver, push, create a pull request, merge, access SQLite or the trusted mirror, or use an unrelated Attempt capability.

The V0 golden path and canary do not invoke delivery, push, pull request creation, or merge.

The base product retains an explicit Boss-only integration and delivery path outside the V0 scope, and that path is not reachable through advisory authority or the V0 canary.

The public report contains bounded outcomes and limitations without credentials, full prompts, transcripts, or raw secret-bearing ledgers.

The RED scope test found that normal daemon boot supervised `NotificationDispatcher` and could write `notifications.log`.

The GREEN fix removes that dispatcher from the V0 application supervision tree while retaining the existing explicit notification module test as out-of-scope legacy behavior.

Command:

    MIX_ENV=test mix test test/consigliere/application_children_test.exs test/consigliere/notification_dispatcher_test.exs --seed 0

Result: 4 passed.

Verdict: PASS for the requested V0 boundary and operator-controlled canary rules.

## Historical superseded exact-head scope refresh for 8d83937

The historical exact source head was `8d839378a55e36222e13c19e84e1f91543fc92c4`.

The follow-up adds no excluded architecture, delivery authority, comparison allocation, telemetry, transcript retention, or legacy supervisor dependency.

It only closes scheduler-cache, advisory-log, and runner-start persistence boundaries with focused tests and bounded process cleanup.

The canary inputs remain operator-controlled, no FirstMate duplicate work was created, and the record remains insufficient for Promote until at least 20 naturally occurring comparable Missions and defensible bounded measurements exist.

Historical verdict: PASS for scope fidelity at that exact local head.

## Historical superseded final exact-head scope refresh for bf22b5d

The historical exact source head was `bf22b5d4cae239a222a3065ca4b34b574dd676ad`.

The final changes remain limited to the ordered V0 revival tasks and their tests, evidence, and packaging proof.

The implementation adds no excluded architecture, delivery authority, comparison allocation, telemetry, transcript retention, fixed canary input, or legacy supervisor dependency.

The canary inputs remain operator-controlled, no FirstMate duplicate work was created, and the record remains insufficient for Promote until at least 20 naturally occurring comparable Missions and defensible bounded measurements exist.

Historical local verdict: PASS for scope fidelity at the exact implementation head.

## Historical superseded exact-source refresh for 7159373

The historical runtime source head was `71593738cf6aae723c9208743405fa12a9dc7a03` on `revival/v0-local-codex`.

The exact-source review found no Capo, Secondmate, remote worker, second production harness, native Codex resume, Made-managed validation, AFK or visible notification path, automatic GitHub delivery, product-created PR, merge, telemetry service, full transcript store, Bash supervisor dependency, fixed Project, fixed allocation, hard duration, duplicate comparison, or automatic Promote path.

Advisory authority remains separate from Boss and delivery authority, and all external work is reconciled outside SQLite transactions.

The selected canary remains one naturally occurring Consigliere Mission with one human-authorized continuation and zero FirstMate duplicate Missions.

The public record explicitly reports insufficient evidence for Promote because the comparable sample is below 20 and leaves the next Continue or Stop decision with the operator.

Historical local F4 verdict: PASS for scope fidelity and operator ownership.

## Historical exact-source scope refresh for 7c54c78

The historical exact implementation source head was `7c54c782552f3ee5a09ddee35735e90cba1b9339` on `revival/v0-local-codex`.

The runtime hardening adds only fail-closed package, generation, process identity, bounded observer, and terminal reconciliation behavior required by the ordered V0 boundary.

It adds no excluded architecture, delivery authority, fixed Project or allocation, hard duration or Continue limit, duplicate comparison work, telemetry service, transcript retention, native resume, or legacy supervisor dependency.

The operator-controlled canary remains unchanged, with one naturally occurring Mission, one human-authorized continuation, zero FirstMate duplicate Missions, and insufficient evidence for Promote.

The operator retains Continue or Stop, and no canary rerun was created after hardening.

The exact-source runtime, package, and canary-custody evidence is recorded in the `7c54c78` receipts and `task-15.md`.

## Historical closing delivery refresh for 85f02c5

The historical pushed product remained within the ordered V0 scope and its exact source runtime head was `7c54c782552f3ee5a09ddee35735e90cba1b9339`.

The separate draft PR head is `85f02c5b739116d7de0d3f04a372f463bbb913e6`, and remote CI passed all five checks.

The canary remains operator-controlled, non-duplicated, and below the 20-Mission threshold.

The public report makes no Promote claim and leaves Continue or Stop with the operator.

The closing scope-fidelity verdict is PASS.

## Historical pushed-head attestation for c060b88

The final pushed head is `c060b88035128bbdbf361f1bcab9a100521965e9`.

The historical delivery delta was documentation/evidence-only after runtime source `7c54c782552f3ee5a09ddee35735e90cba1b9339`.

No excluded integration or automatic canary decision was added, and remote CI run `33322804969` passed all five checks.

The complete final delivery attestation is `final-delivery-attestation-c060b88.md`.

## Historical watcher follow-up exact-head scope fidelity

The follow-up changes modify only the three evidenced watcher gaps and their regressions.

No legacy Bash supervisor, interactive version manager, Capo, persistent repository manager, remote worker, second production harness, native transcript resume, Made behavior, automatic GitHub delivery, telemetry platform, full transcript retention, fixed comparison allocation, duplicate canary, or FirstMate implementation was added.

Runtime source `c727e94ae2bac1be0d3d33fc0005258e5fd850cd` remains exact-SHA bounded, and the selected canary remains below the required 20 comparable Mission records without a Promote claim.

## Historical watcher follow-up security-aligned scope fidelity

The corrected source head `2c7f5b67c8d37737d8f2dd3e0f7110687a800748` changes only the advisory read projection and its regression after the security finding.

It adds no raw-log exposure, authority bypass, duplicate canary, FirstMate implementation, or excluded V0 integration.

The selected canary remains operator-controlled and below the 20-record threshold, so no canary decision was guessed or claimed.

The native command-bound closure remains within the evidenced watcher scope and adds no excluded product integration, canary work, or authority change.

## Historical superseded final exact-candidate scope refresh for 4370995

The final candidate is `43709956c54ab485f9ae077c293f65d269acf27e`.

The candidate adds only the removal of two prose-only test assertions after exact-head review.

No Capo, Secondmate, persistent per-repository manager, remote worker, second production harness, native Codex transcript resume, Made execution, AFK or visible notification path, automatic GitHub delivery, product-created PR, push, merge, telemetry service, full transcript store, fixed Project, fixed allocation, hard duration, duplicate comparison, or automatic Promote path was added or wired into the packaged V0 path.

The unchanged inherited Made, delivery, GitHub, notification, and generic harness modules are present at the historical base but are not invoked by the packaged V0 path; removing them would be unrelated scope expansion.

The canary remains one naturally occurring Consigliere Mission with zero FirstMate duplicate Missions and fewer than 20 comparable records.

The public record therefore makes no Promote claim and leaves Continue or Stop with the operator.

Historical scope-fidelity verdict: PASS for candidate `43709956c54ab485f9ae077c293f65d269acf27e`.

## Historical final-head refresh for 9575670

The historical source head for this superseded refresh was `95756706aa16d00ce0061500363d30bee9446dd8`.

PR 101 remains historical input, PR 141 remains the sole replacement PR, and the canary remains operator-controlled with no Promote claim.

No Made daemon operation, merge, automatic delivery, duplicate FirstMate implementation, or fixed comparison allocation was introduced.

## Historical superseded implementation-head refresh for eb41191

The superseded implementation head for that historical runtime evidence was `eb41191b73a04b93d613d8d0cf8b2183a55272ef`.

The only new product change after the prior evidence refresh is query-level bounding for default project and mission reader lists, with no canary, delivery, Made, or excluded-scope changes.

PR #101 remains historical input, and PR #141 remains the sole draft replacement target.

Historical scope-fidelity verdict: PASS for implementation SHA `eb41191b73a04b93d613d8d0cf8b2183a55272ef`.

## Historical superseded exact implementation refresh

The historical superseded runtime source head is `8010d5fdaa69f9e998b951f8282fddd01e5099ea`.

The only current product changes after `eb41191` are the evidenced Away digest boundary fix and its paged cursor acknowledgement regression.

The canary remains one operator-controlled record below the twenty-Mission Promote threshold, with Continue or Stop retained by the operator and no duplicate work.

The exact package, daemon, cursor, and lifecycle evidence is recorded in the 8010d5f receipts, and no excluded integration, duplicate Mission, Made operation, push, PR creation, or merge was introduced.

Historical scope-fidelity verdict: PASS for runtime source `8010d5fdaa69f9e998b951f8282fddd01e5099ea`, with no canary Promote claim.

## Historical superseded exact implementation refresh for 0fd7d3b

The historical superseded runtime source head is `0fd7d3b951672df7cb37e6c160401d1593386ba2`.

The historical source change was limited to the evidenced monotonic Away cursor fix, its test, and the required exact-head evidence refresh.

The historical package-only QA created no Mission, duplicate canary, FirstMate implementation, Made operation, push, PR, or merge, and made no canary Promote claim.

Historical scope-fidelity verdict: PASS for runtime source `0fd7d3b951672df7cb37e6c160401d1593386ba2`, with no canary Promote claim.

## Historical superseded exact implementation refresh for cf56963

The historical superseded runtime source head is `cf56963a7206e5c5a260442c08eaa7bdcd65ec7a`.

The historical runtime change was limited to the evidenced cancellation ordering fix, its regression coverage, and the required exact-head evidence refresh.

The historical package-only QA created no Mission, duplicate canary, FirstMate implementation, Made operation, push, PR, or merge, and made no canary Promote claim.

Historical scope-fidelity verdict: PASS for runtime source `cf56963a7206e5c5a260442c08eaa7bdcd65ec7a`, with no canary Promote claim.

## Historical superseded exact implementation refresh for ec47784

The historical superseded runtime source head is `ec47784a801ee8168fae7b249bf3b8342951ae17`.

The historical runtime change was limited to the evidenced durable-cause and Away marker-race fixes, their regression coverage, and the required exact-head evidence refresh.

The historical package-only QA created no Mission, duplicate canary, FirstMate implementation, Made operation, push, PR, or merge, and made no canary Promote claim.

Historical scope-fidelity verdict: PASS for runtime source `ec47784a801ee8168fae7b249bf3b8342951ae17`, with no canary Promote claim.

## Current exact implementation refresh

The current runtime source head is `0c2b24c02490c8f6f53b7f6bc1a9fb9add519861`.

The current runtime change is limited to the evidenced unverified-termination reconciliation and Away marker-fencing fixes, their regression coverage, and the required exact-head evidence refresh.

The current package-only QA created no Mission, duplicate canary, FirstMate implementation, Made operation, push, PR, or merge, and makes no canary Promote claim.

Current scope-fidelity verdict: PASS for runtime source `0c2b24c02490c8f6f53b7f6bc1a9fb9add519861`, with no canary Promote claim.

## Current exact-head correction for d63f239

The current runtime source head is `d63f2390944a534f4746c64ef60e43332fd546c3`.

The product delta is limited to the evidenced Away concurrency correction, its real concurrent regression, removal of the temporary DatabaseWriter detour, and the required exact-head evidence receipts.

PR #101 remains historical input, PR #141 remains the separate draft replacement, and no Made operation, delivery, merge, duplicate canary work, or Promote claim was introduced.

Current scope-fidelity verdict: PASS for runtime source `d63f2390944a534f4746c64ef60e43332fd546c3`, pending the fresh exact-head review set.
