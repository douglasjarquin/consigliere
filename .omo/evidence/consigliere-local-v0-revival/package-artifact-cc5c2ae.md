# Package artifact receipt

The package source head was `cc5c2ae368007ec30fba81d74d5a30808176a9d8`.

The build command was `PATH="/opt/homebrew/opt/erlang/bin:$PATH" scripts/package.sh "$PWD/.tmp/package-isolation-r4/prefix"`.

The package command exited `0` after compiling the daemon release and building the native CLI and runner artifacts.

The installed `cs` and `csd` binaries were native arm64 Mach-O executables.

The `cs` SHA-256 was `95c363af48057d223dd09978d24b0add0a94793bca3a8a2d001c94231ff5c222`.

The `csd` SHA-256 was `74cac4cb62916e1441294c95ec9ff58de2d725d6f299d7ceb27bb32c4160a634`.

The OTP release was installed at `libexec/consigliere_daemon` and was exercised by the exact-head real-Codex manual run.

The package contained no source checkout or development-only daemon entrypoint.
