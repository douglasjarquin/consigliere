# Final verification F3: real manual QA

## Final packaged artifact

The package was built from exact committed runtime source head `42933d103da9171c76b1564888e0d6557291fb5d` into `.tmp/package-42933d1.mZakYT`.

The package hashes were `cs` `4b3891c4a27c3c21b10c8324627f2701288cdf83842f8a02619da61df2dd4a2c`, `csd` `400d29f584f7ceacf225608cb28c25ddaf3c9846b19e8e8aa890496afa020b48`, `cs-runner` `fa583582034b6c3aa1c1831f387fe578519172990876c6cf4774c28b7b12a382`, and `cs-attempt` `9393d8fecbadaf680caec85f260443fed17c9a5bdaa76a3ad408d683d95b2acb`.

The installed `cs version --json` response was `{"cs":"0.1.0","protocol":1}`.

All inspected package executables were native arm64 Mach-O binaries.

The package tree contained zero `.go`, `.ex`, `mix.exs`, `mix.lock`, or `go.mod` source-like files.

## Installed user path

The manual driver used only the package prefix, system tools, a fresh `/tmp/cs-final-42933d1-home.e7matc` `CS_HOME`, and a working directory of `/tmp`.

It did not use a source checkout, Mix, the legacy supervisor, or the shared Made daemon.

The driver observed `csd migrate`, `csd start`, live `cs ping`, `cs health`, `cs doctor`, `csd status`, an empty `cs projects` result, two successful idempotent stops, `csd restart`, a new verified daemon PID and lock, a second live `cs ping`, and final `csd stop`.

The final receipt was `F3_INSTALLED_LIFECYCLE stop=verified sockets=0 pid_files=0 owner_files=0 notifications=0 package=/Users/douglasjarquin/.herdr/worktrees/consigliere/cs-consigliere-local-v0-revival/.tmp/package-42933d1.mZakYT runner=fa583582034b6c3aa1c1831f387fe578519172990876c6cf4774c28b7b12a382 attempt=9393d8fecbadaf680caec85f260443fed17c9a5bdaa76a3ad408d683d95b2acb`.

The final stop left `priv.sock`, `api.sock`, `boss.sock`, PID files, owner metadata, notification logs, and package processes absent.

The disposable home cleanup receipt was `CLEANUP=trashed:/tmp/cs-final-42933d1-home.e7matc`; the isolated environment home `/tmp/cs-final-42933d1-env.C2tGuR` was also moved to Trash.

Fresh migration emitted one transient SQLite `database is locked` connection log while the schema lock initialized, then completed successfully; all later lifecycle commands returned success and the final cleanup receipt verified the daemon was stopped.

The complete bounded release receipt is `final-gate-receipt.md`.

## Real Codex terminal path

The real authenticated packaged Codex Mission and its operator-authorized same-task continuation are recorded in `task-15.md` and `docs/v0-canary.md`.

That run used `codex-cli 0.151.0`, harness `codex`, model `gpt-5.6-luna`, reasoning `high`, `workspace-write`, `never` approval, a temporary auth-only Codex home, and bounded per-turn counters.

The first Attempt created and imported checkpoint `861c2932c6f3a868560913b630bafddcceb8e8d8` from the exact trusted base.

The human then authorized one fresh continuation from that exact SHA.

The continuation ended with verified runner death, exact-SHA import, bounded verification digest `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, and `ready_for_review`.

The committed-head live run used the package built from `e2b7fe445e96c356354f31f849e9756b265ecec8`.

Subsequent source changes were bounded process-identity, PATH-independent termination, structured redaction, process-group membership, checkpoint workspace hygiene, evidence hardening, protocol-failure, pause-settlement, scheduler-release, and stream-sequence fixes.

A second live implementation was deliberately not created because the operator-controlled canary forbids duplicate work and fixed allocation.

The final `42933d1` package was therefore verified through the installed release lifecycle, while the real Codex result remains the authoritative non-duplicated terminal proof.

## Manual failure coverage

The canary and task evidence cover rejected Codex arguments, unsupported model configuration, hung validation, missing and conflicting SHAs, stale and unsafe workspaces, process interruption, duplicate terminal reports, restart, repeated stop, and redaction checks.

The final package smoke additionally verified that stop is identity-safe and idempotent after restart.

No credentials, full prompt, transcript, raw Codex configuration, or unredacted logs appear in this record.

Verdict: PASS for the packaged user surface and real terminal proof.

## Current exact-head package and lifecycle

The current exact implementation head is `8d839378a55e36222e13c19e84e1f91543fc92c4`.

The fresh package is `.tmp/package-8d83937.kQYvWF` and its native artifact identities are recorded in `package-artifact-8d83937.log`.

The installed-only lifecycle used the current package from `/tmp` with `env -i`, a fresh temporary CS_HOME, and no source checkout, Mix access, legacy supervisor, or shared Made daemon.

Every lifecycle command returned zero, the restart owner PID and start timestamp changed, and the final receipt reported `F3_INSTALLED_LIFECYCLE stop=verified sockets=0 pid_files=0 owner_files=0 notifications=0 package_processes=0`.

The temporary homes were moved to Trash after final stop and absence checks.

The previously selected real Codex canary remains unchanged and was not rerun because the operator-controlled no-duplicate rule forbids creating another implementation record for the same task.

Current verdict: PASS for exact-head packaged manual QA and canary custody, pending remote exact-head CI evidence after push.

## Final exact-head package and lifecycle refresh

The exact implementation head is `bf22b5d4cae239a222a3065ca4b34b574dd676ad`.

The fresh package prefix and four native artifact hashes are recorded in `package-artifact-bf22b5d.log`.

The installed lifecycle used that package from `/tmp` with `env -i`, a fresh temporary `CS_HOME`, and no source checkout, Mix access, legacy supervisor, or shared Made daemon.

Every lifecycle command returned zero, the restart owner PID changed, and the final receipt reported `F3_INSTALLED_LIFECYCLE stop=verified sockets=0 pid_files=0 owner_files=0 notifications=0 package_processes=0`.

The temporary homes were moved to Trash after final stop and absence checks, as recorded in `installed-lifecycle-bf22b5d.log`.

The selected real Codex canary remains unchanged and was not rerun because the operator-controlled no-duplicate rule forbids another implementation record for the same task.

Final local verdict: PASS for packaged manual QA and canary custody, pending remote exact-head CI evidence after push.

## Current exact-source refresh

The packaged manual-QA target is runtime source `71593738cf6aae723c9208743405fa12a9dc7a03` on `revival/v0-local-codex`.

The fresh report `manual-qa-final-7159373.md` verifies the native package identity, version response, package-only `env -i` lifecycle, migration, start, ping, health, doctor, status, identity-safe stop and restart, repeated-stop idempotence, and cleanup.

The package artifact receipt records native arm64 hashes for `cs`, `csd`, `cs-runner`, and `cs-attempt`.

The selected real canary was not rerun after this package build because duplicate implementation work is prohibited.

Current local F3 verdict: PASS for the installed package surface and canary custody.

## Current exact-source package refresh

The exact implementation source head is `7c54c782552f3ee5a09ddee35735e90cba1b9339` on `revival/v0-local-codex`.

The package receipt `package-artifact-7c54c78.md` records native arm64 identities for `cs`, `csd`, `cs-runner`, `cs-attempt`, and `erlexec`, plus the version and source-like-file scan.

The installed receipt `installed-lifecycle-7c54c78.md` records a fresh package-only `env -i` run from `/tmp` with migration, start, live probes, repeated stop, restart with a changed verified owner, final stop, and Trash cleanup.

The final lifecycle receipt was `F3_INSTALLED_LIFECYCLE stop=verified sockets=0 pid_files=0 owner_files=0 package_processes=0 holder_before=71136 holder_after=71283`.

No real Mission, Attempt, Codex process, or duplicate canary was created in this final package lane because the selected implementation work must not be duplicated.

The exact-source manual-QA report and final runtime audit bind their verdicts to this full source SHA.

## Closing exact-head delivery refresh

The fresh package QA report `manual-qa-final-7c54c78.md` is PASS and is bound to runtime source `7c54c782552f3ee5a09ddee35735e90cba1b9339`.

Its redacted package, transcript, and boundary artifacts verify package-only execution, owner identity, restart, repeated stop, cleanup, and absence of source, legacy supervisor, shared Made, Mission, Attempt, PR, and merge usage.

The package and lifecycle receipts are included in the evidence commit `85f02c5b739116d7de0d3f04a372f463bbb913e6`.

Remote CI run `33322422318` also passed the release smoke lane for the pushed PR head.

The selected real canary was not rerun after hardening because the operator-controlled no-duplicate rule forbids duplicate implementation work.

The closing manual-QA verdict is PASS.

## Final pushed-head attestation

The final pushed head is `c060b88035128bbdbf361f1bcab9a100521965e9`.

The final delivery delta is documentation/evidence-only after the package QA target `7c54c782552f3ee5a09ddee35735e90cba1b9339`.

Remote CI run `33322804969` passed the final release smoke lane and all other checks.

The complete final delivery attestation is `final-delivery-attestation-c060b88.md`.

## Watcher follow-up exact-head manual QA

The exact-head packaged real-Codex run reached `ready_for_review`, returned authorized bounded Attempt logs, passed the existing review and doctor surfaces, and completed repeated stop/restart/stop lifecycle checks.

The final cleanup receipt verified no task-owned package scratch or crash dump remained and no product process remained alive.

The exact identifiers, hashes, commands, bounded outputs, and ruled-out adversarial classes are in `watcher-followup-c727e94.md`.

## Watcher follow-up security-aligned exact-head manual QA

The rebuilt package from source head `2c7f5b67c8d37737d8f2dd3e0f7110687a800748` passed the real Codex terminal path to `ready_for_review`.

The same installed package returned authorized structured Attempt event summaries with `ATTEMPT_LOGS_EXIT=0`, then passed repeated stop, restart, status, why, review, doctor, and final stop checks.

The disposable package prefix, temporary source and home, and QA script are recorded for cleanup and no product process remained after the identity-safe stop.

The cleanup receipt for the current head verified the exact temporary home and package directory were moved to macOS Trash with zero return codes and both paths absent afterward.

The final packaged real-Codex run was rebuilt from runtime source `4e99cf4219998c15b01d23b23349730f27546c61` and passed `ready_for_review`, event-only Attempt logs, lifecycle restart, repeated stop, and cleanup.

The final cleanup receipt verified the temporary package home and QA directory were soft-deleted with `/usr/bin/trash` and were absent afterward.

## Final exact-candidate manual-QA refresh

The final candidate is `43709956c54ab485f9ae077c293f65d269acf27e`.

Its production and package inputs are byte-identical to runtime source `4e99cf4219998c15b01d23b23349730f27546c61`, so the final package QA lane rebuilt and exercised the candidate without creating duplicate Codex or canary work.

The independent installed-artifact lane passed native arm64 package construction, package-only `env -i` operation, real Codex completion to `ready_for_review`, authorized event-only `cs attempt logs`, exact output bounds, restart identity, repeated-stop convergence, and zero final sockets, PID files, owner files, notifications, and package processes.

Its fresh task-owned resources were moved to macOS Trash and verified absent.

Final manual-QA verdict: PASS for candidate `43709956c54ab485f9ae077c293f65d269acf27e`.

## Final head refresh

The final source head is `95756706aa16d00ce0061500363d30bee9446dd8`.

The exact-head package rebuild produced native arm64 artifacts, and the real-Codex packaged transcript reached `ready_for_review` with event-only default Attempt logs.

The refreshed daemon, runner, CLI, and cleanup receipts are recorded under `.omo/evidence/consigliere-local-v0-revival/`.
