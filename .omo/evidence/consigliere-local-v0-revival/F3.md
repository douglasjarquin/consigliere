# Final verification F3: real manual QA

## Final packaged artifact

The package was built from exact committed runtime source head `bc7e980c90aa54e165d5aed3dae060f3a2c9e584` into `.tmp/package-bc7e980-AR4rQ1`.

The package hashes were `cs` `fc5fad8d9fbef0ef2acfe24b5901e3d6d6cc7fc204819ecb5f5aea7de50f55cf`, `csd` `bfd4baaa32c8b2dac5b1578f726652638f72464cf2a3f36dd1f137581e0d8444`, `cs-runner` `c719337bf4ec474260cafb2de9d3e9abf1399b43fc3e1cffe7adccb656223f26`, and `cs-attempt` `9163ce551c9ecd078e8018804351d4666e99b9253d67a2e30bcb120909d3c6f1`.

The installed `cs version --json` response was `{"cs":"0.1.0","protocol":1}`.

All inspected package executables were native arm64 Mach-O binaries.

The package tree contained zero `.go`, `.ex`, `mix.exs`, `mix.lock`, or `go.mod` source-like files.

## Installed user path

The manual driver used only the package prefix, system tools, a fresh `/tmp/cs-final-bc7e980-home.4rOEpA` `CS_HOME`, and a working directory of `/tmp`.

It did not use a source checkout, Mix, the legacy supervisor, or the shared Made daemon.

The driver observed `csd migrate`, `csd start`, live `cs ping`, `cs health`, `cs doctor`, `csd status`, an empty `cs projects` result, two successful idempotent stops, `csd restart`, a new verified daemon PID and lock, a second live `cs ping`, and final `csd stop`.

The final receipt was `F3_INSTALLED_LIFECYCLE stop=verified sockets=0 pid_files=0 owner_files=0 notifications=0 package=/Users/douglasjarquin/.herdr/worktrees/consigliere/cs-consigliere-local-v0-revival/.tmp/package-bc7e980-AR4rQ1 runner=c719337bf4ec474260cafb2de9d3e9abf1399b43fc3e1cffe7adccb656223f26 attempt=9163ce551c9ecd078e8018804351d4666e99b9253d67a2e30bcb120909d3c6f1`.

The final stop left `priv.sock`, `api.sock`, `boss.sock`, PID files, owner metadata, notification logs, and package processes absent.

The disposable home cleanup receipt was `CLEANUP=trashed:/tmp/cs-final-bc7e980-home.4rOEpA`; the isolated environment home `/tmp/cs-final-bc7e980-env.f3TM5S` was also moved to Trash.

Fresh migration emitted one transient SQLite `database is locked` connection log while the schema lock initialized, then completed successfully; all later lifecycle commands returned success and the final cleanup receipt verified the daemon was stopped.

The complete bounded release receipt is `final-gate-receipt.md`.

## Real Codex terminal path

The real authenticated packaged Codex Mission and its operator-authorized same-task continuation are recorded in `task-15.md` and `docs/v0-canary.md`.

That run used `codex-cli 0.151.0`, harness `codex`, model `gpt-5.6-luna`, reasoning `high`, `workspace-write`, `never` approval, a temporary auth-only Codex home, and bounded per-turn counters.

The first Attempt created and imported checkpoint `861c2932c6f3a868560913b630bafddcceb8e8d8` from the exact trusted base.

The human then authorized one fresh continuation from that exact SHA.

The continuation ended with verified runner death, exact-SHA import, bounded verification digest `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, and `ready_for_review`.

The committed-head live run used the package built from `e2b7fe445e96c356354f31f849e9756b265ecec8`.

Subsequent source changes were bounded process-identity, PATH-independent termination, structured redaction, process-group membership, checkpoint workspace hygiene, evidence hardening, protocol-failure, pause-settlement, scheduler-release, and stream-sequence fixes.

A second live implementation was deliberately not created because the operator-controlled canary forbids duplicate work and fixed allocation.

The final `bc7e980` package was therefore verified through the installed release lifecycle, while the real Codex result remains the authoritative non-duplicated terminal proof.

## Manual failure coverage

The canary and task evidence cover rejected Codex arguments, unsupported model configuration, hung validation, missing and conflicting SHAs, stale and unsafe workspaces, process interruption, duplicate terminal reports, restart, repeated stop, and redaction checks.

The final package smoke additionally verified that stop is identity-safe and idempotent after restart.

No credentials, full prompt, transcript, raw Codex configuration, or unredacted logs appear in this record.

Verdict: PASS for the packaged user surface and real terminal proof.
