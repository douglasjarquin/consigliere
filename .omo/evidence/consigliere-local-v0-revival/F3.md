# Final verification F3: real manual QA

## Final packaged artifact

The package was built from committed source head `17ebe767827458b203fd499fad8f46ab070537a6` into `.tmp/package-final-17ebe76.md68yd/prefix`.

The package hashes were `cs` `a301f5ad7cbd18ef8b5aac0ba87aa2103f6c80ab0360857acf3550ec7a39e01b`, `csd` `0197bf0e3561ed7a6adf62b7160616caf51dfbe0fe5627a46da89b1fa7288fbc`, and `cs-runner` `dcd7d161d6c49040ce52fa824b17445928a0b9b0a35a03b636d51303eca68f23`.

The installed `cs version --json` response was `{"cs":"0.1.0","protocol":1}`.

All inspected package executables were native arm64 Mach-O binaries.

## Installed user path

The manual driver used only the package prefix, system tools, a fresh `/tmp/cs-f3-final.ljwxZR` `CS_HOME`, and a working directory of `/tmp`.

It did not use a source checkout, Mix, the legacy supervisor, or the shared Made daemon.

The driver observed `csd migrate`, `csd start`, live `cs ping`, `cs health`, `cs doctor`, `csd status`, an empty `cs projects` result, two successful idempotent stops, `csd restart`, a new verified daemon PID and lock, a second live `cs ping`, and final `csd stop`.

The final receipt was `F3_INSTALLED_LIFECYCLE stop=verified sockets=absent pid=absent`.

The final stop left `priv.sock`, `api.sock`, `boss.sock`, and the PID path absent.

## Real Codex terminal path

The real authenticated packaged Codex Mission and its operator-authorized same-task continuation are recorded in `task-15.md` and `docs/v0-canary.md`.

That run used `codex-cli 0.151.0`, harness `codex`, model `gpt-5.6-luna`, reasoning `high`, `workspace-write`, `never` approval, a temporary auth-only Codex home, and bounded per-turn counters.

The first Attempt created and imported checkpoint `861c2932c6f3a868560913b630bafddcceb8e8d8` from the exact trusted base.

The human then authorized one fresh continuation from that exact SHA.

The continuation ended with verified runner death, exact-SHA import, bounded verification digest `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, and `ready_for_review`.

The committed-head live run used the package built from `e2b7fe445e96c356354f31f849e9756b265ecec8`.

The only source change after that live run is the fail-closed durable progression checkpoint guard covered by the RED/GREEN test in task-13 evidence.

A second live implementation was deliberately not created because the operator-controlled canary forbids duplicate work and fixed allocation.

The final `17ebe76` package was therefore verified through the installed release lifecycle, while the real Codex result remains the authoritative non-duplicated terminal proof.

## Manual failure coverage

The canary and task evidence cover rejected Codex arguments, unsupported model configuration, hung validation, missing and conflicting SHAs, stale and unsafe workspaces, process interruption, duplicate terminal reports, restart, repeated stop, and redaction checks.

The final package smoke additionally verified that stop is identity-safe and idempotent after restart.

No credentials, full prompt, transcript, raw Codex configuration, or unredacted logs appear in this record.

Verdict: PASS for the packaged user surface and real terminal proof.
